{
  "type": "module",
  "scripts": {
    "compile": "pug --client --no-debug src/editor-template.pug -o build"
  },
  "dependencies": {
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "json-stringify-pretty-compact": "^4.0.0",
    "pug-cli": "^1.0.0-alpha6"
  }
}
